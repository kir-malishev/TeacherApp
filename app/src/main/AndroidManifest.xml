<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.teacher"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="21" />
<uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@drawable/diary"
        android:label="@string/app_name"
       android:theme="@style/AppTheme"
       >
        <activity
            android:name="app.teacher.LoginActivity"
            android:windowSoftInputMode="stateHidden"
            android:label="@string/app_name"
            android:noHistory="true"
             >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="app.teacher.MainActivity"
            android:windowSoftInputMode="stateHidden"
            android:label="@string/test"
            android:noHistory="true" >
            <intent-filter>

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="app.teacher.RegActivity"
            android:windowSoftInputMode="stateHidden"
            android:label="@string/registration"
            android:noHistory="true" >
            <intent-filter>

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            </activity>
                    <activity
            android:name="app.teacher.EditTestActivity"
            android:windowSoftInputMode="stateHidden"
            android:label="@string/create"
            android:noHistory="false" >
            <intent-filter>

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            </activity>
          <activity
            android:name="app.teacher.EditChoiceActivity"
            android:windowSoftInputMode="stateHidden"
            android:label="@string/create"
            android:noHistory="true" >
            <intent-filter>

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
                  <activity
            android:name="app.teacher.EditInputActivity"
            android:windowSoftInputMode="stateHidden"
            android:label="@string/create"
            android:noHistory="true" >
            <intent-filter>

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity

            android:name="app.teacher.EditMultipleActivity"
            android:windowSoftInputMode="stateHidden"
            android:label="@string/create"
            android:noHistory="true" >
            <intent-filter>

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        	<activity
        	 android:name="app.teacher.ActivityMenu"
        	 android:windowSoftInputMode="stateHidden"
    			android:label="@string/menu"
    			android:noHistory="true">
    			<intent-filter>
                
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
					</activity>
					
					        	<activity
        	 android:name="app.teacher.MyTestsActivity"
        	 android:windowSoftInputMode="stateHidden"
    			android:label="@string/mytests"
    			android:noHistory="true">
    			<intent-filter>
                
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
					</activity>
					
						<activity
        	 android:name="app.teacher.TestReviewActivity"
        	 android:windowSoftInputMode="stateHidden"
    			android:label="@string/mytests"
    			android:noHistory="true">
    			<intent-filter>
                
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
					</activity>
					<activity
            android:name="app.teacher.ResultsActivity"
            android:windowSoftInputMode="stateHidden"
            android:label="@string/result"
            android:noHistory="false" >
                			<intent-filter>
                
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            </activity>
            <activity
            android:name="app.teacher.ResultViewActivity"
            android:windowSoftInputMode="stateHidden"
            android:label="@string/result"
            android:noHistory="true" >
                			<intent-filter>
                
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            </activity>
            </application>

</manifest>
